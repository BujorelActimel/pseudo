citeste n # (numar natural nenul)
nr<-0; i<-1
cat timp i<=n executa
    x<-0; y<-1; j<-1
    cat timp j<i executa
        r<-2*x-y; x<-y; y<-r
        j<-j+1
    sf
    i<-i+1
    daca y>0 atunci
        nr<-nr+1
    sf
sf
scrie nr
