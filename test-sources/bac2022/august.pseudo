citeste m,n # (numere naturale nenule, m<n)
x <- 1
cat timp x=1 executa
    x<-m
    y<-n
    n<-n-1
    cat timp x!=y executa
       daca x>y atunci x<-x-y
         altfel y<-y-x
       sf
    sf
sf
scrie n+1
