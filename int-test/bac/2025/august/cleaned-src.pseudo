citeste x,y
#  (numere naturale distincte)
 cx<-0; cy<-0
cat timp x>=10 sau y>=10 executa
    cat timp x+y!=0 executa
        cx<-cx+x%10; x<-[x/10]
        cy<-cy+y%10; y<-[y/10]
    sf
    x<-cx; cx<-0; y<-cy; cy<-0
sf
daca x=y atunci scrie "DA ",x
    altfel scrie "NU ",x," ",y
sf
